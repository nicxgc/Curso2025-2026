# -*- coding: utf-8 -*-
"""Task06_2025.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1GKS6qAm9T4y76LWtB8ZP-ToTFBSDENE_

**Task 06: Modifying RDF(s)**
"""

#pip install rdflib
import urllib.request
url = 'https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/refs/heads/master/Assignment4/course_materials/python/validation.py'
urllib.request.urlretrieve(url, 'validation.py')
github_storage = "https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/master/Assignment4/course_materials"

"""Import RDFLib main methods"""

from rdflib import Graph, Namespace, Literal, XSD
from rdflib.namespace import RDF, RDFS
from validation import Report
g = Graph()
g.namespace_manager.bind('ns', Namespace("http://somewhere#"), override=False)
r = Report()

"""Create a new class named Researcher"""

ns = Namespace("http://mydomain.org#")
g.add((ns.Researcher, RDF.type, RDFS.Class))
for s, p, o in g:
  print(s,p,o)

"""**Task 6.0: Create new prefixes for "ontology" and "person" as shown in slide 14 of the Slidedeck 01a.RDF(s)-SPARQL shown in class.**"""

# this task is validated in the next step
ontology_ns = Namespace("http://oeg.fi.upm.es/def/people#")
person_ns = Namespace("http://oeg.fi.upm.es/resource/person/")

g.namespace_manager.bind('ontology', ontology_ns, override=True)
g.namespace_manager.bind('person', person_ns, override=True)

"""**TASK 6.1: Reproduce the taxonomy of classes shown in slide 34 in class (all the classes under "Vocabulario", Slidedeck: 01a.RDF(s)-SPARQL). Add labels for each of them as they are in the diagram (exactly) with no language tags. Remember adding the correct datatype (xsd:String) when appropriate**

"""

class_hierarchy = [
    (ontology_ns.Person, None, "Person"),
    (ontology_ns.Professor, ontology_ns.Person, "Professor"),
    (ontology_ns.AssociateProfessor, ontology_ns.Professor, "AssociateProfessor"),
    (ontology_ns.FullProfessor, ontology_ns.Professor, "FullProfessor"),
    (ontology_ns.InterimAssociateProfessor, ontology_ns.AssociateProfessor, "InterimAssociateProfessor")
]

for class_uri, parent_class, label in class_hierarchy:
    g.add((class_uri, RDF.type, RDFS.Class))
    g.add((class_uri, RDFS.label, Literal(label, datatype=XSD.string)))
    if parent_class:
        g.add((class_uri, RDFS.subClassOf, parent_class))
        
# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_01(g)

"""**TASK 6.2: Add the 3 properties shown in slide 36. Add labels for each of them (exactly as they are in the slide, with no language tags), and their corresponding domains and ranges using RDFS. Remember adding the correct datatype (xsd:String) when appropriate. If a property has no range, make it a literal (string)**"""

properties = [
    (ontology_ns.hasColleague, "hasColleague", ontology_ns.Person, ontology_ns.Person),
    (ontology_ns.hasName, "hasName", ontology_ns.Person, RDFS.Literal),
    (ontology_ns.hasHomePage, "hasHomePage", ontology_ns.FullProfessor, RDFS.Literal)
]

for prop_uri, label, domain, range_ in properties:
    g.add((prop_uri, RDF.type, RDF.Property))
    g.add((prop_uri, RDFS.label, Literal(label, datatype=XSD.string)))
    g.add((prop_uri, RDFS.domain, domain))
    g.add((prop_uri, RDFS.range, range_))

# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_02(g)

"""**TASK 6.3: Create the individuals shown in slide 36 under "Datos". Link them with the same relationships shown in the diagram."**"""

individuals_data = [
    (person_ns.Oscar, ontology_ns.AssociateProfessor, "Oscar", "Óscar Corcho García", None, person_ns.Asun),
    (person_ns.Asun, ontology_ns.FullProfessor, "Asun", None, "http://oeg.fi.upm.es/", person_ns.Raul),
    (person_ns.Raul, ontology_ns.InterimAssociateProfessor, "Raul", None, None, None)
]

for individual, type_class, label, name, homepage, colleague in individuals_data:
    g.add((individual, RDF.type, type_class))
    g.add((individual, RDFS.label, Literal(label, datatype=XSD.string)))
    
    if name:
        g.add((individual, ontology_ns.hasName, Literal(name, datatype=XSD.string)))
    
    if homepage:
        g.add((individual, ontology_ns.hasHomePage, Literal(homepage, datatype=XSD.string)))
    
    if colleague:
        g.add((individual, ontology_ns.hasColleague, colleague))

# Visualize the results
for s, p, o in g:
  print(s,p,o)

r.validate_task_06_03(g)

"""**TASK 6.4: Add to the individual person:Oscar the email address, given and family names. Use the properties already included in example 4 to describe Jane and John (https://raw.githubusercontent.com/FacultadInformatica-LinkedData/Curso2025-2026/master/Assignment4/course_materials/rdf/example4.rdf). Do not import the namespaces, add them manually**

"""

FOAF = Namespace("http://xmlns.com/foaf/0.1/")
VCARD = Namespace("http://www.w3.org/2001/vcard-rdf/3.0/")

g.namespace_manager.bind('foaf', FOAF, override=False)
g.namespace_manager.bind('vcard', VCARD, override=False)

contact_info = [
    (VCARD.Given, "Óscar"),
    (VCARD.Family, "Corcho García"),
    (FOAF.email, "ocorcho@fi.upm.es")
]

for property, value in contact_info:
    g.add((person_ns.Oscar, property, Literal(value, datatype=XSD.string)))
    
# Visualize the results
for s, p, o in g:
  print(s,p,o)

# Validation. Do not remove
r.validate_task_06_04(g)
r.save_report("_Task_06")